CREATE INDEX index_companies_id ON companies (id);

CREATE INDEX index_vacancies_id ON vacancies (id);

CREATE INDEX index_users_email ON users (email);

CREATE INDEX index_users_id ON users (id);

CREATE INDEX index_roles_name ON roles (name);

CREATE INDEX index_roles_id ON roles (id);

CREATE INDEX index_applications_id ON applications (id);


